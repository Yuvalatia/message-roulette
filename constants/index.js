const rooms = {
  ALL_CONNECTED_SOCKETS_ROOM: 'socket:all'
}

const events = {
  PRIVATE_MESSAGE_EVENT: 'privateMessage',
  BROADCAST_MESSAGE_EVENT: 'broadcastMessage'
}

module.exports = {
  rooms,
  events
}
